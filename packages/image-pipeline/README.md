# Ozon 第 7 步：图片检查

图片管线下载并解码 1688 原图，按真实字节计算 SHA-256，检查协议、响应、
格式、损坏、大小、像素、比例、重复内容和 SKU 绑定。当前 Agent 根据
`agent_tasks` 目视判断中文文字与水印，并用内容哈希提交
`ImageReviewAgentInputV1`；未审核时状态为 `needs_review`。

生图是可选能力。未配置生图模型时继续使用合格的 1688 原图；配置后默认
生成三张图，并记录 provider、模型、调用 ID、提示词版本及参考图使用情况。
任何 API Key 都不得写入产物或 Git。
