{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://auto-ozon.local/schemas/ozon-publish-result-v1.json",
  "type": "object",
  "required": ["schema_version", "run_id", "request_sha256", "task_ids", "status", "submitted_at", "completed_at", "items"],
  "properties": { "schema_version": { "const": 1 }, "status": { "enum": ["succeeded", "partial", "failed"] } }
}
