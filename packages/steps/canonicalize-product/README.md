# Canonical product step

Owns transformation of sanitized 1688 offer facts into CanonicalProductV2,
SKU assembly, shared/varying field analysis, integrity checks, and the
`02-canonical` run artifact. It never opens a browser or performs collection.

Public entry: `runCanonicalizeProduct`.
