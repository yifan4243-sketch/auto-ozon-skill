# 1688 source step

Owns input validation, deep collection dispatch, safe raw-result normalization,
and the `01-source` run artifact. Browser sessions, search, official similar
lookup, and offer-page collection remain in `@auto-ozon/adapters-1688`.

Public entry: `runSource1688`.
